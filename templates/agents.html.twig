<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genesys Helpdesk | Register Agent</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
    <div class="app-container">
       
       {{ include('partials/_sidebar.html.twig') }}

        <main class="main-content">
            <div class="card" style="margin-bottom: 2rem;">
                <div class="card-header">
                    <h3 class="card-title">Register New Agent</h3>
                    <p class="card-subtitle">Add a new support agent to the helpdesk system.</p>
                </div>

                <form action="/submit-agent" method="POST" class="form-stack">
                    <div class="form-group">
                        <label class="label">Agent Name</label>
                        <input type="text" name="name" required placeholder="e.g. Sarah Connor" class="input">
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label class="label">Email</label>
                            <input type="email" name="email" required placeholder="agent@genesys.com" class="input">
                        </div>
                        <div class="form-group">
                            <label class="label">Password</label>
                            <input type="password" name="password" required placeholder="••••••••" class="input">
                        </div>
                    </div>

                    <div class="alert-box alert-emerald">
                        <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        <div>
                            <span style="font-weight: bold; display: block; margin-bottom: 0.25rem;">Status Note</span>
                            New agents are saved with the role of <strong>agent</strong> and can be assigned to tickets immediately.
                        </div>
                    </div>

                    <div class="btn-group">
                        <button type="reset" class="btn btn-reset">Reset</button>
                        <button type="submit" class="btn btn-submit">Register Agent</button>
                    </div>
                </form>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Active Agents</h3>
                    <p class="card-subtitle">Agents currently available in the system.</p>
                </div>

                <div class="table-container">
                    <table class="styled-table">
                        <thead>
                            <tr>
                                <th>Agent ID</th>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Role</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for agent in agents %}
                                <tr>
                                    <td>#AG-{{ agent.id }}</td>
                                    <td>{{ agent.name }}</td>
                                    <td>{{ agent.email }}</td>
                                    <td>{{ agent.role }}</td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="4">No agents registered yet.</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
