<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genesys Helpdesk | Assign Ticket</title>
<link rel="stylesheet" href="{{ asset('css/style.css') }}"></head>
<body>
    <div class="app-container">
     
     {{ include('partials/_sidebar.html.twig') }}

        <main class="main-content">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Ticket Assignment</h3>
                    <p class="card-subtitle">Link a support ticket to a specific agent or technician.</p>
                </div>

                <form action="/submit-assignment" method="POST" class="form-stack">
                    <input type="hidden" name="assignment_id" value="">

                    <div class="form-row">
                        <div class="form-group">
                            <label class="label">Ticket ID</label>
                            <input type="text" name="ticket_id" required placeholder="e.g. T-2024-885" class="input">
                            <p class="helper-text">Reference ID of the ticket to be assigned.</p>
                        </div>
                        <div class="form-group">
                            <label class="label">Agent ID</label>
                            <input type="text" name="agent_id" required placeholder="e.g. AGT-007" class="input">
                             <p class="helper-text">ID of the technician taking ownership.</p>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="label">Date Assignation</label>
                        <input type="date" name="date_assignation" required class="input">
                    </div>

                    <div class="alert-box alert-blue">
                        <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        <div>
                            <span style="font-weight: bold; display: block; margin-bottom: 0.25rem;">Assignment Note</span>
                            Assigning a ticket will trigger an email notification to the agent and update the ticket status to "In Progress".
                        </div>
                    </div>

                    <div class="btn-group">
                        <button type="reset" class="btn btn-reset">Reset</button>
                        <button type="submit" class="btn btn-submit">Confirm Assignment</button>
                    </div>
                </form>
            </div>
        </main>
    </div>
    <script>document.querySelector('input[name="date_assignation"]').valueAsDate = new Date();</script>
</body>
</html>
